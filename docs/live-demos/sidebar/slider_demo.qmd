---
title: "{{< iconify ph sliders-horizontal >}} Slider Demo"
format: dashboard
---

```{r, include=FALSE}
# Initialize dashboardr page configuration (CSS/JS for charts)
dashboardr::.page_config()
```

```{r, echo=FALSE, results='asis'}
dashboardr::enable_inputs()
```

```{r, echo=FALSE, results='asis'}
dashboardr::enable_sidebar()
```

```{r setup}
#| echo: false
#| warning: false
#| message: false
#| error: false
#| results: 'hide'

# Load dashboardr (includes dplyr, highcharter as dependencies)
library(dashboardr)

# Global chunk options
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  error = FALSE,
  fig.width = 12,
  fig.height = 8,
  dpi = 300
)

# Load data
data <- readRDS('dataset_small_35obs.rds')

# Data summary
cat('Dataset loaded:', nrow(data), 'rows,', ncol(data), 'columns\n')

```


## {.sidebar width="280px"}

### Year Range


Use the slider to filter the timeline starting from a specific year.


---


```{r}
#| echo: false
#| results: 'asis'
dashboardr::render_input(
  input_id = "year_slider",
  label = "Start Year:",
  type = "slider",
  filter_var = "year",
  options = NULL,
  options_from = NULL,
  default_selected = NULL,
  placeholder = "Select...",
  width = "300px",
  min = 2018,
  max = 2024,
  step = 1,
  value = 2018,
  show_value = TRUE,
  inline = TRUE,
  stacked = FALSE,
  stacked_align = "center",
  group_align = "left",
  ncol = NULL,
  nrow = NULL,
  toggle_series = NULL,
  override = FALSE,
  labels = NULL,
  size = "md",
  help = NULL,
  disabled = FALSE
)
```


<div style='height: 1rem;'></div>


```{r}
#| echo: false
#| results: 'asis'
dashboardr::render_input(
  input_id = "country_filter3",
  label = "Countries:",
  type = "checkbox",
  filter_var = "country",
  options = c("Netherlands", "Germany", "France", "United Kingdom", "Italy"),
  options_from = NULL,
  default_selected = c("Netherlands", "Germany", "France"),
  placeholder = "Select...",
  width = "300px",
  min = 0,
  max = 100,
  step = 1,
  value = NULL,
  show_value = TRUE,
  inline = TRUE,
  stacked = FALSE,
  stacked_align = "center",
  group_align = "left",
  ncol = NULL,
  nrow = NULL,
  toggle_series = NULL,
  override = FALSE,
  labels = NULL,
  size = "md",
  help = NULL,
  disabled = FALSE
)
```


---


::: {.callout-tip}
Move the slider to filter data from a specific year onwards.
:::


## Column

### GDP Growth Over Time

```{r timeline-value-country-3}
# GDP Growth Over Time
result <- viz_timeline(
  data = data,
  title = "GDP Growth Over Time",
  time_var = "year",
  y_var = "value",
  group_var = "country",
  agg = "none",
  chart_type = "line",
  y_label = "Growth Rate (%)",
  x_label = "Year",
  cross_tab_filter_vars = c("year", "country")
)

result <- dashboardr::.embed_cross_tab(result)

result
```

