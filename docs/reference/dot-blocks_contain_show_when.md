# Recursively check if any content block has show_when set

Recursively check if any content block has show_when set

## Usage

``` r
.blocks_contain_show_when(blocks)
```

## Arguments

- blocks:

  List of content blocks (may contain nested content collections)

## Value

Logical
