# Enable URL Parameter Deep Linking

Adds JavaScript that reads filter state from URL query parameters on
page load and updates the URL as filters change. Enables shareable
dashboard URLs with pre-set filters and tab navigation via hash
fragments.

## Usage

``` r
enable_url_params()
```

## Value

HTML script tag for url_params.js
