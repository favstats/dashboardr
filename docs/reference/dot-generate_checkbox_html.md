# Generate HTML for checkbox input

Generate HTML for checkbox input

## Usage

``` r
.generate_checkbox_html(
  input_id,
  label,
  filter_var,
  options,
  default_selected,
  width,
  align,
  inline,
  size = "md",
  help = NULL,
  disabled = FALSE,
  columns = NULL
)
```
