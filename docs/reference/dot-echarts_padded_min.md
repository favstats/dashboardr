# JavaScript function for echarts4r y-axis min with padding. Returns a JS function that computes dataMin minus ~5% of the data range, giving a comfortable buffer below the lowest value.

JavaScript function for echarts4r y-axis min with padding. Returns a JS
function that computes dataMin minus ~5% of the data range, giving a
comfortable buffer below the lowest value.

## Usage

``` r
.echarts_padded_min()
```

## Value

An htmlwidgets::JS object
