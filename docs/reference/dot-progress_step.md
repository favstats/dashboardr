# Show custom progress step

Show custom progress step

## Usage

``` r
.progress_step(
  msg,
  elapsed = NULL,
  show_progress = TRUE,
  is_last = FALSE,
  use_page_style = FALSE
)
```

## Arguments

- msg:

  Step message

- elapsed:

  Optional elapsed time in seconds

- show_progress:

  Whether to show progress
