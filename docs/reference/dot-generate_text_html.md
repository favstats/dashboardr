# Generate HTML for text input

Generate HTML for text input

## Usage

``` r
.generate_text_html(
  input_id,
  label,
  filter_var,
  value,
  placeholder,
  width,
  align,
  size = "md",
  help = NULL,
  disabled = FALSE
)
```
