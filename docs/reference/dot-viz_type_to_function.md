# Look up viz function name from type

Look up viz function name from type

## Usage

``` r
.viz_type_to_function(type)
```

## Arguments

- type:

  Character string, the viz type name (e.g., "bar", "donut").

## Value

Character string, the function name (e.g., "viz_bar").
