# Build tooltip from legacy prefix/suffix parameters

Build tooltip from legacy prefix/suffix parameters

## Usage

``` r
.build_tooltip_from_legacy(prefix, suffix, x_suffix, chart_type, context)
```
