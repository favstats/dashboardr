# Generate page configuration HTML

Internal helper that outputs CSS and JavaScript for chart containers and
reflow. Called from generated QMD setup chunks.

## Usage

``` r
.page_config()
```

## Value

Invisible NULL (outputs HTML via knitr)
