# Start a sparkline card row

Creates a container for sparkline cards displayed side by side.

## Usage

``` r
add_sparkline_card_row(content, tabgroup = NULL, show_when = NULL)
```

## Arguments

- content:

  Content collection or page_object

- tabgroup:

  Optional tabgroup

- show_when:

  Optional conditional display formula
