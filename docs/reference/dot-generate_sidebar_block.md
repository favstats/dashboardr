# Internal function to generate markdown for a sidebar

Internal function to generate markdown for a sidebar

## Usage

``` r
.generate_sidebar_block(sidebar, page = NULL)
```

## Arguments

- sidebar:

  Sidebar content block with blocks, width, position, title, and styling

- page:

  Page object (for data access)

## Value

Character vector of markdown lines
