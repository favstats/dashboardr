# Recursively check if any spec in a list has show_when set

Recursively check if any spec in a list has show_when set

## Usage

``` r
.specs_contain_show_when(specs)
```

## Arguments

- specs:

  List of visualization specs (may contain nested_children)

## Value

Logical
