# Build CSS margin style string from individual margin parameters

Build CSS margin style string from individual margin parameters

## Usage

``` r
.build_margin_style(mt = NULL, mr = NULL, mb = NULL, ml = NULL)
```
