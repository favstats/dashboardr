# Generate input block markdown

Internal function to generate markdown for input filter widgets

## Usage

``` r
.generate_input_block(block, page = NULL)
```

## Arguments

- block:

  Input content block

- page:

  Page object (for data access)

## Value

Character vector of markdown lines
