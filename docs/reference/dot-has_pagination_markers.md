# Check if page has pagination markers

Internal function to detect if a page contains pagination markers

## Usage

``` r
.has_pagination_markers(page)
```

## Arguments

- page:

  Page object

## Value

Logical indicating if page has pagination
