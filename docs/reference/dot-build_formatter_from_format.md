# Build formatter JavaScript from a format string with placeholders

Build formatter JavaScript from a format string with placeholders

## Usage

``` r
.build_formatter_from_format(format_string, chart_type, context)
```
