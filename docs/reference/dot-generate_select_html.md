# Generate HTML for select input

Generate HTML for select input

## Usage

``` r
.generate_select_html(
  input_id,
  label,
  type,
  filter_var,
  options,
  default_selected,
  placeholder,
  width,
  align,
  size = "md",
  help = NULL,
  disabled = FALSE
)
```
