# Generate input row block markdown

Internal function to generate markdown for a row of input widgets

## Usage

``` r
.generate_input_row_block(block, page = NULL)
```

## Arguments

- block:

  Input row content block

- page:

  Page object (for data access)

## Value

Character vector of markdown lines
