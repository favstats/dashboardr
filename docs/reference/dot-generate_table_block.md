# Generate table block markdown

Internal function to generate markdown for table content blocks (data
frames using knitr::kable)

## Usage

``` r
.generate_table_block(block)
```

## Arguments

- block:

  Table content block

## Value

Character vector of markdown lines
