# Enable Sidebar Styling

Adds modern CSS styling for sidebar panels. Called automatically when a
page includes a sidebar via add_sidebar().

## Usage

``` r
enable_sidebar()
```

## Value

HTML tags to include sidebar CSS

## Examples

``` r
if (FALSE) { # \dontrun{
# Usually called automatically, but can be added manually:
enable_sidebar()
} # }
```
