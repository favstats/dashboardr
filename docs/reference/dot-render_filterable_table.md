# Render a filterable static table with embedded data

Render a filterable static table with embedded data

## Usage

``` r
.render_filterable_table(data, table_id, caption = NULL, filter_vars = NULL)
```
