# Generate HTML for switch input

Generate HTML for switch input

## Usage

``` r
.generate_switch_html(
  input_id,
  label,
  filter_var,
  value,
  width,
  align,
  toggle_series = NULL,
  override = FALSE,
  size = "md",
  help = NULL,
  disabled = FALSE
)
```
