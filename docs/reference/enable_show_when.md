# Enable show_when (conditional visibility) script only

Includes the JavaScript that evaluates `data-show-when` on viz
containers. Called automatically when a page has visualizations with
`show_when` but no inputs.

## Usage

``` r
enable_show_when()
```

## Value

HTML script tag for show_when.js
