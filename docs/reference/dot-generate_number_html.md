# Generate HTML for number input

Generate HTML for number input

## Usage

``` r
.generate_number_html(
  input_id,
  label,
  filter_var,
  min,
  max,
  step,
  value,
  width,
  align,
  size = "md",
  help = NULL,
  disabled = FALSE
)
```
