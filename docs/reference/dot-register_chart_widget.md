# Register a chart widget with the dashboardr JS registry

Register a chart widget with the dashboardr JS registry

## Usage

``` r
.register_chart_widget(widget, backend, chart_id = NULL, filter_vars = NULL)
```
