# Wrap block markdown with show_when helpers

Wrap block markdown with show_when helpers

## Usage

``` r
.wrap_show_when_block(lines, show_when)
```

## Arguments

- lines:

  Character vector of markdown lines

- show_when:

  Optional show_when formula
