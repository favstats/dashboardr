# Generate modal block markdown

Internal function to generate markdown for modal content blocks

## Usage

``` r
.generate_modal_block(block)
```

## Arguments

- block:

  Modal content block with modal_id and html_content

## Value

Character vector of markdown lines
