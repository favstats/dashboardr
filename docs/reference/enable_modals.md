# Enable Modal Functionality

Adds modal CSS and JavaScript to enable clickable links that open
content in a centered modal overlay instead of navigating to a new page.

## Usage

``` r
enable_modals()
```

## Value

HTML tags to include modal functionality

## Examples

``` r
if (FALSE) { # \dontrun{
# In your dashboard page content:
enable_modals()
} # }
```
