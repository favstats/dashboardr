# Generate HTML for button group input

Generate HTML for button group input

## Usage

``` r
.generate_button_group_html(
  input_id,
  label,
  filter_var,
  options,
  default_selected,
  width,
  align,
  size = "md",
  help = NULL,
  disabled = FALSE
)
```
