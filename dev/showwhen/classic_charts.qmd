---
title: "Classic Charts"
format: dashboard
---

```{r, include=FALSE}
# Initialize dashboardr page configuration (CSS/JS for charts)
dashboardr:::.page_config()
```

```{r, echo=FALSE, results='asis'}
dashboardr::enable_inputs(show_when = TRUE)
```

```{r, echo=FALSE, results='asis'}
dashboardr::enable_sidebar()
```

```{r setup}
#| echo: false
#| warning: false
#| message: false
#| error: false
#| results: 'hide'

# Load dashboardr (includes dplyr, highcharter as dependencies)
library(dashboardr)

# Global chunk options
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  error = FALSE,
  fig.width = 12,
  fig.height = 8,
  dpi = 300
)

# Conditional-visibility helpers (fallback for older package versions)
if (!exists('show_when_open', mode = 'function')) {
  show_when_open  <- function(j) cat(paste0('<div class="viz-show-when" data-show-when=\'', j, '\'>\n'))
  show_when_close <- function()  cat('</div>\n')
}

# Load data
data <- readRDS('dataset_small_400obs.rds')

# Data summary
cat('Dataset loaded:', nrow(data), 'rows,', ncol(data), 'columns\n')

```


## {.sidebar width="280px"}

### Controls


```{r}
#| echo: false
#| results: 'asis'
dashboardr::render_input(
  input_id = "chart1",
  label = "Select Chart",
  type = "select_single",
  filter_var = "chart_type",
  options = c("Bar", "Scatter", "Density", "Boxplot", "Histogram"),
  options_from = NULL,
  default_selected = "Bar",
  placeholder = "Select...",
  width = "300px",
  min = 0,
  max = 100,
  step = 1,
  value = NULL,
  show_value = TRUE,
  inline = TRUE,
  stacked = FALSE,
  stacked_align = "center",
  group_align = "left",
  ncol = NULL,
  nrow = NULL,
  toggle_series = NULL,
  override = FALSE,
  labels = NULL,
  size = "md",
  help = NULL,
  disabled = FALSE
)
```


## Column

### Bar Chart

```{r bar-category}
#| results: 'asis'
# Bar Chart
show_when_open('{"var":"chart_type","op":"eq","val":"Bar"}')
result <- viz_bar(
  data = data,
  title = "Bar Chart",
  x_var = "category"
)

result <- dashboardr:::.embed_cross_tab(result)

result
show_when_close()
```

### Scatter Plot

```{r scatter-plot}
#| results: 'asis'
# Scatter Plot
show_when_open('{"var":"chart_type","op":"eq","val":"Scatter"}')
result <- viz_scatter(
  data = data,
  title = "Scatter Plot",
  x_var = "x_continuous",
  y_var = "y_continuous"
)

result <- dashboardr:::.embed_cross_tab(result)

result
show_when_close()
```

### Density Plot

```{r density-value}
#| results: 'asis'
# Density Plot
show_when_open('{"var":"chart_type","op":"eq","val":"Density"}')
result <- viz_density(
  data = data,
  title = "Density Plot",
  x_var = "value"
)

result <- dashboardr:::.embed_cross_tab(result)

result
show_when_close()
```

### Boxplot

```{r boxplot-value-category}
#| results: 'asis'
# Boxplot
show_when_open('{"var":"chart_type","op":"eq","val":"Boxplot"}')
result <- viz_boxplot(
  data = data,
  title = "Boxplot",
  x_var = "category",
  y_var = "value"
)

result <- dashboardr:::.embed_cross_tab(result)

result
show_when_close()
```

### Histogram

```{r histogram-score}
#| results: 'asis'
# Histogram
show_when_open('{"var":"chart_type","op":"eq","val":"Histogram"}')
result <- viz_histogram(
  data = data,
  title = "Histogram",
  x_var = "score"
)

result <- dashboardr:::.embed_cross_tab(result)

result
show_when_close()
```

