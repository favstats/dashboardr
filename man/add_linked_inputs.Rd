% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_collection.R
\name{add_linked_inputs}
\alias{add_linked_inputs}
\title{Add linked parent-child inputs (cascading dropdowns)}
\usage{
add_linked_inputs(x, parent, child, type = "select")
}
\arguments{
\item{x}{A sidebar_container (from \code{add_sidebar()}).}

\item{parent}{List with: \code{id}, \code{label}, \code{options}; optionally
\code{default_selected}, \code{filter_var}.}

\item{child}{List with: \code{id}, \code{label}, \code{options_by_parent}
(named list mapping each parent value to a character vector of child options);
optionally \code{filter_var}.}

\item{type}{Input type for parent: \code{"select"} (default) or \code{"radio"}.}
}
\value{
The modified sidebar_container for piping.
}
\description{
Creates two linked select inputs where the child's available options depend on
the parent's current selection. Use inside a sidebar (after \code{add_sidebar()}).
}
\examples{
\dontrun{
add_sidebar() \%>\%
  add_linked_inputs(
    parent = list(id = "dimension", label = "Dimension",
                  options = c("AI", "Safety", "Digital Health")),
    child = list(id = "question", label = "Question",
                 options_by_parent = list(
                   "AI" = c("Overall", "Using AI Tools"),
                   "Safety" = c("Overall", "Passwords", "Phishing"),
                   "Digital Health" = c("Overall", "Screen Time")
                 ))
  ) \%>\%
  end_sidebar()
}
}
