% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_collection.R
\name{create_content}
\alias{create_content}
\title{Create a new content/visualization collection (alias for create_viz)}
\usage{
create_content(tabgroup_labels = NULL, ...)
}
\arguments{
\item{tabgroup_labels}{Named vector/list mapping tabgroup IDs to display names}

\item{...}{Default parameters to apply to all subsequent add_viz() calls}
}
\value{
A content_collection (also a viz_collection for compatibility)
}
\description{
This is an alias for \code{\link{create_viz}} - both functions are identical.
Use whichever name makes more sense for your use case. The returned collection
can be built up with any combination of add_viz(), add_text(), and add_image().
}
\details{
Note: Both names return the same object with both "content_collection" and
"viz_collection" classes for backward compatibility.
}
\examples{
\dontrun{
# These are equivalent:
content <- create_content() \%>\%
  add_text("# Title") \%>\%
  add_viz(type = "histogram", x_var = "age")

content <- create_viz() \%>\%
  add_text("# Title") \%>\%
  add_viz(type = "histogram", x_var = "age")
}
}
