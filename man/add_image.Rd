% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_collection.R
\name{add_image}
\alias{add_image}
\title{Add image to content collection (pipeable)}
\usage{
add_image(
  content_collection = NULL,
  src,
  alt = NULL,
  caption = NULL,
  width = NULL,
  height = NULL,
  align = c("center", "left", "right"),
  link = NULL,
  class = NULL
)
}
\arguments{
\item{content_collection}{A content_collection, viz_collection, or NULL}

\item{src}{Image source path or URL}

\item{alt}{Alt text for the image}

\item{caption}{Optional caption text displayed below the image}

\item{width}{Optional width (e.g., "300px", "50\%", "100\%")}

\item{height}{Optional height (e.g., "200px")}

\item{align}{Image alignment: "left", "center", "right" (default: "center")}

\item{link}{Optional URL to link the image to}

\item{class}{Optional CSS class for custom styling}
}
\value{
Updated content_collection object
}
\description{
Adds an image block to a content collection. Can be used standalone or in a pipe.
Supports viz_collection as first argument for seamless piping.
}
\examples{
\dontrun{
# Standalone
img <- add_image(src = "logo.png", alt = "Logo")

# Pipe with content
content <- create_content() \%>\%
  add_text("Welcome!") \%>\%
  add_image(src = "chart.png", alt = "Chart")

# Pipe directly from viz
content <- create_viz() \%>\%
  add_viz(type = "bar", x_var = "category") \%>\%
  add_image(src = "logo.png", alt = "Logo")
}
}
