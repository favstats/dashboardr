% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_server.R
\name{dashboardr_mcp_server}
\alias{dashboardr_mcp_server}
\title{Start dashboardr MCP Server}
\usage{
dashboardr_mcp_server(
  backend = c("mcptools", "mcpr"),
  transport = c("stdio", "http"),
  port = 8080L
)
}
\arguments{
\item{backend}{Character, which MCP backend to use. \code{"mcptools"} (default,
Posit-maintained) or \code{"mcpr"} (alternative). If the chosen backend is not
installed, falls back to the other.}

\item{transport}{Character, either \code{"stdio"} (default) or \code{"http"}.}

\item{port}{Integer, port for HTTP transport (default 8080). Ignored when
\code{transport = "stdio"}.}
}
\value{
Called for its side effect (starts a blocking MCP server). Does not
return.
}
\description{
Launches an MCP server that exposes dashboardr documentation, function
reference, example code, and visualization guides to LLM-powered coding
assistants like Claude Desktop, Claude Code, Cursor, and VS Code Copilot.
}
\details{
Tools are defined using \code{\link[ellmer:tool]{ellmer::tool()}} and served via
\code{\link[mcptools:server]{mcptools::mcp_server()}} (default) or \code{\link[mcpr:serve_io]{mcpr::serve_io()}} as a fallback.

The server exposes five tools:
\describe{
\item{dashboardr_guide}{Returns the full dashboardr API guide covering
the three-layer architecture, function index, and quick start.}
\item{dashboardr_function_help}{Look up detailed help for any exported
dashboardr function.}
\item{dashboardr_list_functions}{List exported functions, optionally
filtered by category.}
\item{dashboardr_example}{Get runnable example code for common dashboard
patterns.}
\item{dashboardr_viz_types}{Quick reference of all visualization types
with key parameters and use cases.}
}
}
\section{Configuration}{


\strong{Claude Code:}

\if{html}{\out{<div class="sourceCode">}}\preformatted{claude mcp add dashboardr -- Rscript -e "dashboardr::dashboardr_mcp_server()"
}\if{html}{\out{</div>}}

\strong{Claude Desktop} (\code{claude_desktop_config.json}):

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "mcpServers": \{
    "dashboardr": \{
      "command": "Rscript",
      "args": ["-e", "dashboardr::dashboardr_mcp_server()"]
    \}
  \}
\}
}\if{html}{\out{</div>}}

\strong{VS Code / Cursor:}

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "mcp": \{
    "servers": \{
      "dashboardr": \{
        "type": "stdio",
        "command": "Rscript",
        "args": ["-e", "dashboardr::dashboardr_mcp_server()"]
      \}
    \}
  \}
\}
}\if{html}{\out{</div>}}
}

