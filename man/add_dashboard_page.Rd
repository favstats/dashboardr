% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dashboard_creation.R
\name{add_dashboard_page}
\alias{add_dashboard_page}
\title{Add a page to the dashboard}
\usage{
add_dashboard_page(
  proj,
  name,
  data = NULL,
  data_path = NULL,
  template = NULL,
  params = list(),
  visualizations = NULL,
  content = NULL,
  text = NULL,
  icon = NULL,
  is_landing_page = FALSE,
  show_in_nav = TRUE,
  tabset_theme = NULL,
  tabset_colors = NULL,
  navbar_align = c("left", "right"),
  overlay = FALSE,
  overlay_theme = c("light", "glass", "dark", "accent"),
  overlay_text = "Loading",
  overlay_duration = 2200,
  lazy_load_charts = NULL,
  lazy_load_margin = NULL,
  lazy_load_tabs = NULL,
  lazy_debug = NULL,
  pagination_separator = NULL,
  time_var = NULL,
  slug = NULL
)
}
\arguments{
\item{proj}{A dashboard_project object}

\item{name}{Page display name}

\item{data}{Optional data frame to save for this page. Can also be a named list of data frames
for using multiple datasets: \code{list(survey = df1, demographics = df2)}}

\item{data_path}{Path to existing data file (alternative to data parameter). Can also be a named
list of file paths for multiple datasets}

\item{template}{Optional custom template file path}

\item{params}{Parameters for template substitution}

\item{visualizations}{Content collection or list of visualization specs}

\item{content}{Alternative to visualizations - supports content collections}

\item{text}{Optional markdown text content for the page}

\item{icon}{Optional iconify icon shortcode (e.g., "ph:users-three")}

\item{is_landing_page}{Whether this should be the landing page (default: FALSE)}

\item{show_in_nav}{Whether to show this page in the navbar (default: TRUE).
Set to FALSE for pageless dashboards (created with \code{create_page("")}).}

\item{tabset_theme}{Optional tabset theme for this page (overrides dashboard-level theme)}

\item{tabset_colors}{Optional tabset colors for this page (overrides dashboard-level colors)}

\item{navbar_align}{Position of page in navbar: "left" (default) or "right"}

\item{overlay}{Whether to show a loading overlay on page load (default: FALSE)}

\item{overlay_theme}{Theme for loading overlay: "light", "glass", "dark", or "accent" (default: "light")}

\item{overlay_text}{Text to display in loading overlay (default: "Loading")}

\item{overlay_duration}{Duration in milliseconds for how long overlay stays visible (default: 2200)}

\item{lazy_load_charts}{Override dashboard-level lazy loading setting for this page (default: NULL = inherit from dashboard)}

\item{lazy_load_margin}{Override viewport margin for lazy loading on this page (default: NULL = inherit from dashboard)}

\item{lazy_load_tabs}{Override tab-aware lazy loading for this page (default: NULL = inherit from dashboard)}

\item{lazy_debug}{Override debug mode for lazy loading on this page (default: NULL = inherit from dashboard)}

\item{pagination_separator}{Text to show in pagination navigation (e.g., "of" -> "1 of 3"), default: NULL = inherit from dashboard}

\item{time_var}{Name of the time/x-axis column in the data (e.g., "year", "decade", "date").
Used by input filters when switching metrics. If NULL (default), the JavaScript will try to
auto-detect from common column names (year, decade, time, date).}

\item{slug}{Optional custom slug for the page filename. If provided, overrides the
default name-based slug. Non-alphanumeric characters are replaced with underscores.}
}
\value{
The updated dashboard_project object
}
\description{
Universal function for adding any type of page to the dashboard. Can create
landing pages, analysis pages, about pages, or any combination of text and
visualizations. All content is markdown-compatible.
}
\examples{
\dontrun{
# Landing page
dashboard <- create_dashboard("test") \%>\%
  add_page("Welcome", text = "# Welcome\n\nThis is the main page.", is_landing_page = TRUE)

# Analysis page with data and visualizations
dashboard <- dashboard \%>\%
  add_page("Demographics", data = survey_data, visualizations = demo_viz)

# Text-only about page
dashboard <- dashboard \%>\%
  add_page("About", text = "# About This Study\n\nThis dashboard shows...")

# Mixed content page
dashboard <- dashboard \%>\%
  add_page("Results", text = "# Key Findings\n\nHere are the results:",
           visualizations = results_viz, icon = "ph:chart-line")

# Page with explicit time variable for metric switching
dashboard <- dashboard \%>\%
  add_page("Trends", data = trend_data, visualizations = trend_viz, time_var = "decade")
}
}
