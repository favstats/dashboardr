% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_collection.R
\name{add_filter}
\alias{add_filter}
\title{Add a filter control (simplified interface)}
\usage{
add_filter(
  content,
  filter_var,
  label = NULL,
  type = c("checkbox", "select", "radio"),
  ...
)
}
\arguments{
\item{content}{A content_collection object}

\item{filter_var}{The column name in your data to filter by (quoted or unquoted)}

\item{label}{Optional label for the filter (defaults to the column name)}

\item{type}{Filter type: "checkbox" (default), "select", or "radio"}

\item{...}{Additional arguments passed to \code{\link{add_input}}}
}
\value{
Updated content_collection
}
\description{
A convenience wrapper around \code{\link{add_input}} for common filtering use cases.
Options are automatically derived from the data column specified by \code{filter_var}.
All values are selected by default.
}
\examples{
\dontrun{
# Simplest usage - just specify the column!
content <- create_content(data = mydata) \%>\%
  add_sidebar() \%>\%
    add_filter(filter_var = "education") \%>\%
    add_filter(filter_var = "gender") \%>\%
  end_sidebar() \%>\%
  add_viz(type = "stackedbar", x_var = "region", stack_var = "outcome")
}
}
