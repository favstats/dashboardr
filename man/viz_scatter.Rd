% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_scatter.R
\name{viz_scatter}
\alias{viz_scatter}
\title{Create Scatter Plot}
\usage{
viz_scatter(
  data,
  x_var,
  y_var,
  color_var = NULL,
  size_var = NULL,
  title = NULL,
  subtitle = NULL,
  x_label = NULL,
  y_label = NULL,
  color_palette = NULL,
  point_size = 4,
  show_trend = FALSE,
  trend_method = "lm",
  alpha = 0.7,
  include_na = FALSE,
  na_label = "(Missing)",
  tooltip = NULL,
  tooltip_format = NULL,
  jitter = FALSE,
  jitter_amount = 0.2
)
}
\arguments{
\item{data}{A data frame containing the data.}

\item{x_var}{Character string. Name of the variable for the x-axis (continuous or categorical).}

\item{y_var}{Character string. Name of the variable for the y-axis (continuous).}

\item{color_var}{Optional character string. Name of grouping variable for coloring points.}

\item{size_var}{Optional character string. Name of variable to control point sizes.}

\item{title}{Optional main title for the chart.}

\item{subtitle}{Optional subtitle for the chart.}

\item{x_label}{Optional label for the x-axis. Defaults to \code{x_var} name.}

\item{y_label}{Optional label for the y-axis. Defaults to \code{y_var} name.}

\item{color_palette}{Optional character vector of colors for the points.}

\item{point_size}{Numeric. Default size for points when \code{size_var} is not specified. Defaults to 4.}

\item{show_trend}{Logical. Whether to add a trend line. Defaults to \code{FALSE}.}

\item{trend_method}{Character string. Method for trend line: "lm" (linear) or "loess". Defaults to "lm".}

\item{alpha}{Numeric between 0 and 1. Transparency of points. Defaults to 0.7.}

\item{include_na}{Logical. Whether to include NA values in color grouping. Defaults to \code{FALSE}.}

\item{na_label}{Character string. Label for NA category if \code{include_na = TRUE}. Defaults to "(Missing)".}

\item{tooltip}{A tooltip configuration created with \code{\link{tooltip}()},
OR a format string with \{placeholders\}. Available placeholders:
\code{\{x\}}, \code{\{y\}}, \code{\{name\}}, \code{\{series\}}.
See \code{\link{tooltip}} for full customization options.}

\item{tooltip_format}{Character string. Custom format for tooltips using Highcharts
placeholders like \{point.x\}, \{point.y\}. For the simpler dashboardr placeholder
syntax, use the \code{tooltip} parameter instead.}

\item{jitter}{Logical. Whether to add jittering to reduce overplotting. Defaults to \code{FALSE}.}

\item{jitter_amount}{Numeric. Amount of jittering if \code{jitter = TRUE}. Defaults to 0.2.}
}
\value{
A highcharter plot object.
}
\description{
Creates interactive scatter plots showing relationships between two continuous variables.
Supports optional color grouping, custom sizing, and trend lines.
}
\examples{
# Simple scatter plot
plot1 <- viz_scatter(
  data = mtcars,
  x_var = "wt",
  y_var = "mpg",
  title = "Car Weight vs MPG"
)
plot1

# Scatter plot with color grouping
plot2 <- viz_scatter(
  data = iris,
  x_var = "Sepal.Length",
  y_var = "Sepal.Width",
  color_var = "Species",
  title = "Iris Sepal Measurements"
)
plot2

# Scatter with trend line and custom colors
plot3 <- viz_scatter(
  data = mtcars,
  x_var = "hp",
  y_var = "mpg",
  color_var = "cyl",
  show_trend = TRUE,
  title = "Horsepower vs MPG by Cylinders",
  color_palette = c("#FF6B6B", "#4ECDC4", "#45B7D1")
)
plot3

}
