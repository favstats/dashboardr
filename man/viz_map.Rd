% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_map.R
\name{viz_map}
\alias{viz_map}
\title{Create an interactive map visualization}
\usage{
viz_map(
  data,
  map_type = "custom/world",
  value_var,
  join_var = "iso2c",
  title = NULL,
  subtitle = NULL,
  legend_title = NULL,
  color_stops = NULL,
  color_palette = c("#f7fbff", "#08306b"),
  na_color = "#E0E0E0",
  click_url_template = NULL,
  click_var = NULL,
  tooltip = NULL,
  tooltip_vars = NULL,
  tooltip_format = NULL,
  height = 500,
  border_color = "#FFFFFF",
  border_width = 0.5,
  credits = FALSE,
  backend = "highcharter",
  ...
)
}
\arguments{
\item{data}{Data frame with geographic data}

\item{map_type}{Map type: "custom/world", "custom/world-highres",
"countries/us/us-all", etc. See Highcharts map collection.}

\item{value_var}{Column name for color values (required)}

\item{join_var}{Column name to join with map geography (default: "iso2c")}

\item{title}{Chart title}

\item{subtitle}{Chart subtitle}

\item{legend_title}{Legend title (defaults to value_var)}

\item{color_stops}{Numeric vector of color gradient stops}

\item{color_palette}{Character vector of colors for gradient}

\item{na_color}{Color for missing/NA values (default: "#E0E0E0")}

\item{click_url_template}{URL template for click navigation.
Use \code{\{var\}} syntax for variable substitution, e.g.,
"\{iso2c\}_dashboard/index.html"}

\item{click_var}{Variable to use in click URL (defaults to join_var)}

\item{tooltip}{A tooltip configuration created with \code{\link{tooltip}()},
OR a format string with \{placeholders\}. Available placeholders:
\code{\{name\}}, \code{\{value\}}.
See \code{\link{tooltip}} for full customization options.}

\item{tooltip_vars}{Character vector of variables to show in tooltip (legacy).}

\item{tooltip_format}{Custom tooltip format string using Highcharts syntax (legacy).
For the simpler dashboardr placeholder syntax, use \code{tooltip} instead.}

\item{height}{Chart height in pixels (default: 500)}

\item{border_color}{Border color between regions (default: "#FFFFFF")}

\item{border_width}{Border width (default: 0.5}

\item{credits}{Show Highcharts credits (default: FALSE)}

\item{backend}{Rendering backend: "highcharter" (default), "plotly", "echarts4r", or "ggiraph".}

\item{...}{Additional arguments passed to highcharter::hcmap()}
}
\value{
A highchart object
}
\description{
Creates a Highcharter choropleth map with optional click navigation
to other pages/dashboards. Designed to work within the dashboardr
visualization system via add_viz(type = "map", ...).
}
\examples{
\dontrun{
# Basic world map
country_data <- data.frame(
  iso2c = c("US", "DE", "FR"),
  total_ads = c(1000, 500, 300)
)

viz_map(
  data = country_data,
  value_var = "total_ads",
  join_var = "iso2c",
  title = "Ad Spending by Country"
)

# With click navigation to country dashboards
viz_map(
  data = country_data,
  value_var = "total_ads",
  click_url_template = "{iso2c}_dashboard/index.html",
  title = "Click a country to explore"
)

# Using with add_viz()
viz <- create_viz() \%>\%
  add_viz(
    type = "map",
    value_var = "total_ads",
    join_var = "iso2c",
    click_url_template = "{iso2c}_dashboard/index.html"
  )
}
}
