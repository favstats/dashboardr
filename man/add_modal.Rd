% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modal_helpers.R
\name{add_modal}
\alias{add_modal}
\title{Add Modal to Content Collection (Pipeable)}
\usage{
add_modal(
  x,
  modal_id,
  title = NULL,
  modal_content = NULL,
  image = NULL,
  image_width = "100\%",
  ...
)
}
\arguments{
\item{x}{A content_collection, viz_collection, or page_object to add modal to}

\item{modal_id}{Unique ID for this modal (used in markdown link)}

\item{title}{Modal title (optional)}

\item{modal_content}{Text content - can be plain text, HTML, or data.frame}

\item{image}{Optional image URL or path}

\item{image_width}{Width of the image (default "100\%"). Can be percentage ("70\%") or pixels ("500px")}

\item{...}{Additional content (data.frames will be converted to tables)}
}
\value{
Updated content_collection with modal added
}
\description{
Adds a modal definition to your content collection. Use markdown links
with \{.modal-link\} class to trigger the modal.
}
\examples{
\dontrun{
# Pipeable syntax (RECOMMENDED)
content <- create_content() \%>\%
  add_text("## Results") \%>\%
  add_text("[View details](#details){.modal-link}") \%>\%
  add_modal(
    modal_id = "details",
    title = "Full Results",
    modal_content = "Detailed analysis here..."
  )

# With image (custom width)
content <- create_viz() \%>\%
  add_viz(type = "column", x_var = "x", y_var = "y") \%>\%
  add_modal(
    modal_id = "chart-details",
    title = "Chart Details",
    image = "chart.png",
    image_width = "70\%",  # Control image width
    modal_content = "This chart shows..."
  )

# With data.frame (auto-converts to table)
content <- create_content() \%>\%
  add_text("[View data](#data){.modal-link}") \%>\%
  add_modal(
    modal_id = "data",
    title = "Raw Data",
    modal_content = head(mtcars, 10)
  )

# Works with page objects too
page <- create_page("Results", data = my_data, type = "bar") \%>\%
  add_text("[View details](#info){.modal-link}") \%>\%
  add_modal(
    modal_id = "info",
    title = "More Info",
    modal_content = "Additional details..."
  )
}
}
