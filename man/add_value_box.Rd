% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_collection.R
\name{add_value_box}
\alias{add_value_box}
\title{Add a custom styled value box}
\usage{
add_value_box(
  content,
  title,
  value,
  logo_url = NULL,
  logo_text = NULL,
  bg_color = "#2c3e50",
  description = NULL,
  description_title = "About this source",
  tabgroup = NULL
)
}
\arguments{
\item{content}{Content collection object or value_box_row_container}

\item{title}{Box title (small text above value)}

\item{value}{Main value to display (large text)}

\item{logo_url}{Optional URL or path to logo image}

\item{logo_text}{Optional text to display as logo (if no logo_url)}

\item{bg_color}{Background color (hex code), default "#2c3e50"}

\item{description}{Optional collapsible description text (markdown supported)}

\item{description_title}{Title for collapsible section, default "About this source"}

\item{tabgroup}{Optional tabgroup for organizing content (character vector for nested tabs)}
}
\description{
Creates a modern value box with optional logo, custom background color,
and optional collapsible description. Perfect for displaying KPIs and metrics
with additional context.
}
\details{
Can be used standalone or within a value box row:
\itemize{
\item Standalone: create_content() \%>\% add_value_box(...)
\item In row: create_content() \%>\% add_value_box_row() \%>\% add_value_box(...) \%>\% add_value_box(...)
}
}
\examples{
\dontrun{
# Standalone value box
content <- create_content() \%>\%
  add_value_box(
    title = "Total Revenue",
    value = "EUR 1,234,567",
    logo_text = "$",
    bg_color = "#2E86AB"
  )
  
# Row of value boxes (pipeable!)
content <- create_content() \%>\%
  add_value_box_row() \%>\%
    add_value_box(title = "Users", value = "1,234") \%>\%
    add_value_box(title = "Revenue", value = "EUR 56K")
}
}
