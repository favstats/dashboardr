% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dashboard.R
\name{by_gender}
\alias{by_gender}
\title{Generate Bar Chart of Means by Gender}
\usage{
by_gender(
  data,
  var1,
  var2,
  thevarlabs,
  thelabel = "<- 1: never     -     7: very often ->"
)
}
\arguments{
\item{data}{A data frame with survey data. Must include a \code{geslacht} (gender) column.}

\item{var1}{The starting variable name (unquoted) for the range.}

\item{var2}{The ending variable name (unquoted) for the range.}

\item{thevarlabs}{A character vector of labels for the variables.}

\item{thelabel}{A character string for the y-axis label.
Defaults to "<- 1: never     -     7: very often ->".}
}
\value{
A \code{highchart} object.
}
\description{
This function creates a grouped bar chart displaying the means of
specified variables, broken down by gender. It includes error bars
for standard errors.
}
\details{
Steps:
1. Maps variable names to \code{thevarlabs}.
2. Converts \code{geslacht} to labels (using \code{sjmisc::to_label}).
3. Selects variables \code{var1:var2} and \code{geslacht}, then removes NAs.
(Commented code suggests internationalization of gender labels and reordering).
4. Calculates mean, SE, and N for each variable, grouped by \code{geslacht}.
5. Joins variable labels and orders them based on global \code{theorder}.
6. Generates a grouped column chart using \code{highcharter}:
- Columns are means per gender within each variable.
- Error bars (mean +/- SE) are linked.
- Y-axis is 1-7.
- Legend for gender is shown.
- Caption includes N and error bar details.
}
\examples{
# Assuming 'survey_data' has 'q1'-'q5', 'geslacht',
# and 'var_labels' for 'q1'-'q5'. `theorder` should exist.
# by_gender(survey_data, q1, q5, var_labels)

Dependencies: dplyr, tidyr, highcharter, forcats, sjmisc
              (Note: `std.error` function defined elsewhere; `theorder` global variable)
}
