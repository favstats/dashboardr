% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dashboard.R
\name{generate_general2}
\alias{generate_general2}
\title{Generate a General Summary Bar Chart for Three Specific Variables}
\usage{
generate_general2(
  data,
  var1,
  var2,
  var3,
  thevarlabs,
  thelabel = "<- 1: never     -     7: very often ->"
)
}
\arguments{
\item{data}{A data frame containing the survey data.}

\item{var1}{The first variable name (unquoted).}

\item{var2}{The second variable name (unquoted).}

\item{var3}{The third variable name (unquoted).}

\item{thevarlabs}{A character vector of three labels for \code{var1}, \code{var2}, \code{var3} respectively.}

\item{thelabel}{A character string for the y-axis label.
Defaults to "<- 1: never     -     7: very often ->".}
}
\value{
A \code{highchart} object.
}
\description{
This function is similar to \code{generate_general} but is designed
to work with three explicitly named variables (\code{var1}, \code{var2}, \code{var3})
instead of a range. It creates a bar chart of means with error bars.
}
\details{
The function:
1. Selects the three specified variables.
2. Removes rows with NAs.
3. Calculates mean, standard error, and count for each of the three variables.
4. Assigns \code{thevarlabs}.
5. Reorders variables by mean (descending) and updates global \code{theorder}.
6. Generates a \code{highcharter} column chart with means and error bars (mean +/- SE).
Y-axis is 1-7. Caption includes N.
}
\examples{
# generate_general2(survey_data, q1, q2, q3, c("Label Q1", "Label Q2", "Label Q3"))

Dependencies: dplyr, tidyr, highcharter, forcats
              (Note: `std.error` function defined elsewhere)
}
