% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_core.R
\name{.as_var_string}
\alias{.as_var_string}
\title{Convert a variable argument to a string (supports both quoted and unquoted syntax)}
\usage{
.as_var_string(var)
}
\arguments{
\item{var}{A quosure captured with \code{rlang::enquo()}}
}
\value{
Character string of the variable name, or NULL if the input was NULL
}
\description{
Internal helper that enables tidy evaluation for variable parameters.
Accepts both \code{x_var = "degree"} (quoted) and \code{x_var = degree} (unquoted).
}
\examples{
\dontrun{
# Inside a function:
my_func <- function(x_var) {
  x_var <- .as_var_string(rlang::enquo(x_var))
  # x_var is now always a character string
}

my_func("degree")  # returns "degree"
my_func(degree)    # returns "degree"
}
}
\keyword{internal}
