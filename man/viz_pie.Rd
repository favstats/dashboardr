% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_pie.R
\name{viz_pie}
\alias{viz_pie}
\title{Create a Pie or Donut Chart}
\usage{
viz_pie(
  data,
  x_var,
  y_var = NULL,
  title = NULL,
  subtitle = NULL,
  inner_size = "0\%",
  color_palette = NULL,
  x_order = NULL,
  sort_by_value = FALSE,
  data_labels_enabled = TRUE,
  data_labels_format = "{point.name}: {point.percentage:.1f}\%",
  show_in_legend = TRUE,
  weight_var = NULL,
  include_na = FALSE,
  na_label = "(Missing)",
  tooltip = NULL,
  tooltip_prefix = "",
  tooltip_suffix = "",
  center_text = NULL
)
}
\arguments{
\item{data}{A data frame containing the data.}

\item{x_var}{Character string. Name of the categorical variable (slice labels).}

\item{y_var}{Optional character string. Name of a numeric column with pre-aggregated
values. When provided, skips counting and uses these values directly.}

\item{title}{Optional main title for the chart.}

\item{subtitle}{Optional subtitle for the chart.}

\item{inner_size}{Character string. Size of the inner hole as percentage (e.g., "50\%").
Set to "0\%" for a standard pie chart (default), or "40\%"-"70\%" for a donut chart.}

\item{color_palette}{Optional character vector of colors for the slices.}

\item{x_order}{Optional character vector specifying the order of slices.}

\item{sort_by_value}{Logical. If TRUE, sort slices by value (largest first). Default FALSE.}

\item{data_labels_enabled}{Logical. If TRUE (default), show labels on slices.}

\item{data_labels_format}{Character string. Format for data labels.
Default shows name and percentage: "\{point.name\}: \{point.percentage:.1f\}\%".}

\item{show_in_legend}{Logical. If TRUE (default), show a legend.}

\item{weight_var}{Optional character string. Name of a weight variable for weighted counts.}

\item{include_na}{Logical. Whether to include NA as a category. Default FALSE.}

\item{na_label}{Character string. Label for the NA category. Default "(Missing)".}

\item{tooltip}{A tooltip configuration created with \code{\link{tooltip}()},
OR a format string with \{placeholders\}. Available placeholders:
\code{\{name\}}, \code{\{value\}}, \code{\{percent\}}.
See \code{\link{tooltip}} for full customization options.}

\item{tooltip_prefix}{Optional string prepended to tooltip values.}

\item{tooltip_suffix}{Optional string appended to tooltip values.}

\item{center_text}{Optional character string. Text to display in the center of a donut chart.
Only visible when inner_size > "0\%".}
}
\value{
A highcharter plot object.
}
\description{
Creates an interactive pie or donut chart using highcharter.
Pie charts show proportional data as slices of a circle.
Donut charts are pie charts with a hollow center.
}
\examples{
\dontrun{
# Simple pie chart
viz_pie(mtcars, x_var = "cyl", title = "Cars by Cylinders")

# Donut chart
viz_pie(mtcars, x_var = "cyl", inner_size = "50\%", title = "Donut Chart")

# Pre-aggregated data
df <- data.frame(category = c("A", "B", "C"), count = c(40, 35, 25))
viz_pie(df, x_var = "category", y_var = "count")
}
}
